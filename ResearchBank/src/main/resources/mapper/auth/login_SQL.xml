<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">

	<select id="loginCheck" resultType="String">
		SELECT MEMBER_ID, MEMBER_PW, MEMBER_NICKNAME,ROLE_ID FROM MEMBER_LIST
		WHERE 
			MEMBER_ID = #{member_id},					<!-- 회원 아이디 -->
			MEMBER_PW = #{member_pw},					<!-- 회원 비밀번호 -->
	</select>
	<select id="viewMember" resultType="com.kh.researchbank.Auth.Login.vo.LoginVO">
        SELECT 
            MEMBER_ID,
            MEMBER_PW,
            MEMBER_NICKNAME,
            ROLE_ID
        FROM MEMBER_LIST
        WHERE MEMBER_ID = #{member_id} AND MEMBER_PW = #{member_pw}
    </select>
</mapper>
